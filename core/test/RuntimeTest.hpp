#ifndef GNURADIO_CORE_TEST_RUNTIME_TEST_HPP
#define GNURADIO_CORE_TEST_RUNTIME_TEST_HPP

// The harness lives in the installed headers so that block libraries can build their own qa on it. This alias lets the
// tests in this directory reach it by name.
#include <gnuradio-4.0/RuntimeTest.hpp>

#endif // GNURADIO_CORE_TEST_RUNTIME_TEST_HPP
